# Copie para .env e ajuste os valores conforme o ambiente.

DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Banco PostgreSQL (deixe DB_NAME em branco para usar o SQLite local)
DB_NAME=getit
DB_USER=getituser
DB_PASSWORD=getitsenha
DB_HOST=localhost
DB_PORT=5432
